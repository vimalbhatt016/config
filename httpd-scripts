======= Script to clear memory ============
#!/bin/bash
status=`free -g |awk '{print $3}' |grep -v free |head -n 1`
if [ $status -gt 15 ];then
echo `/etc/init.d/httpd restart`
else
echo "OK"
fi
=========== Script to restart httpd=========

#!/bin/bash
comm=`ps -efl | grep httpd | wc -l`
if [ $comm -gt 800 ];then
echo `killall -9 httpd; sleep 2 ; /etc/init.d/httpd start` 
else 
echo ok
fi


+++++++++++++++++++============Apache hanged +++++++++++===================================

#!/bin/bash/
service httpd stop;service httpd status;sleep;service httpd start 
#killall -9 httpd;sleep 2;ipcs -s | grep apache | perl -e 'while (<STDIN>) { @a=split(/\s+/);  print `ipcrm sem $a[1]`}';`pkill  -9 httpd`;service httpd restart;sleep 5;service httpd status;curl localhost
watch 'uptime;ip r;df -h ;free -g'
